package constants

const (
	// TodoApiVersion is the version of the todo-api service
	TodoApiVersion = "v0.0.1"
	// TodoPortNumber contains the port number of the todo-api service
	TodoPortNumber = ":8080"
	// TodoStatusHealthy is the status healthy string
	TodoStatusHealthy = "Status: healthy \n"
)
